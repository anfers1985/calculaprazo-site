name: Agente MTE

on:
  schedule:
    - cron: '30 10 * * 1-5'   # 07h30 da manhã em Brasília
  workflow_dispatch:

jobs:
  publicar:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: pip install requests python-slugify

      - name: Executar Agente MTE
        env:
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
        run: python scripts/agentes/agente_mte.py

      - name: Publicar no GitHub
        run: |
          git config user.name "Agente MTE"
          git config user.email "bot@calculaprazo.com.br"
          git add -A
          if git diff --staged --quiet; then
            echo "Nenhuma mudança para commitar."
          else
            git commit -m "MTE: $(date '+%d/%m/%Y %H:%M')"
            git push
          fi
