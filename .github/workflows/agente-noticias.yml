name: Agente Noticias
on:
  schedule:
    - cron: '0 16 * * 1-5'  # 13h Brasília, dias úteis
  workflow_dispatch:

jobs:
  publicar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests python-slugify
      - name: Executar agente noticias
        env:
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
        run: python scripts/agentes/agente_noticias.py
      - name: Publicar
        run: |
          git config user.name "Agente Noticias"
          git config user.email "bot@calculaprazo.com.br"
          git add -A
          git diff --staged --quiet || git commit -m "Noticias: $(date '+%d/%m/%Y %H:%M')"
          git push
